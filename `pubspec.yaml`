
---

## File 3 — `pubspec.yaml`

**Path:** `mobile-tv-studio/pubspec.yaml`

```yaml
name: mobile_tv_studio
description: One app that turns phones into a TV-style broadcast studio.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.10

  # State management
  provider: ^6.1.1

  # Storage / settings
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2

  # Permissions
  permission_handler: ^11.3.0

  # Camera
  camera: ^0.10.5+9

  # WebRTC (local Wi-Fi + internet relay)
  flutter_webrtc: ^0.10.6

  # RTMP streaming out to YouTube / Facebook / Twitch
  rtmp_broadcaster: ^0.0.5

  # Networking
  web_socket_channel: ^2.4.5
  http: ^1.2.0

  # Bluetooth (Android only — no-op on iOS)
  flutter_bluetooth_serial: ^0.4.0
  flutter_blue_plus: ^1.32.0

  # Ads / commercial break player
  video_player: ^2.8.3
  chewie: ^1.7.4

  # Local recording
  ffmpeg_kit_flutter: ^6.0.3

  # Utilities
  uuid: ^4.3.3
  intl: ^0.19.0
  logger: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
